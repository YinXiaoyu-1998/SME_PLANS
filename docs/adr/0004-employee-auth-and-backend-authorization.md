# 员工账号鉴权与后端强制权限过滤

第一阶段员工账号是唯一权限主体，MCP、CLI、网页或其他工具都继承员工账号权限，不创建独立智能体身份。所有查询入口必须由企业资料中枢后端统一执行权限过滤，不能依赖前端、CLI、MCP 或员工智能体自行过滤；无权限资料对普通员工和员工智能体表现为不存在。

**Considered Options**: 为每个智能体或应用单独授权；由各入口自行过滤资料。

**Consequences**: 工具接入更简单，权限边界集中在后端，但访问令牌、查询和下载都必须可审计。
