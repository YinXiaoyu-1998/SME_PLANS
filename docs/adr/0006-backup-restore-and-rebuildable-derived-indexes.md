# 备份恢复与可重建派生索引

企业资料中枢把 MySQL 中的权威元数据和对象存储中的文件本体视为必须保护的数据；派生索引可以从二者重建。第一阶段建议 MySQL 做每日全量备份并结合 binlog/PITR，对象存储开启版本保留或生命周期保护；向量索引和派生表不作为不可丢失的权威数据。

**Considered Options**: 对所有索引做同等级强备份；不区分权威数据和派生数据。

**Consequences**: 备份恢复方案更简单，成本更低，但需要确保派生索引构建流程可重复执行。
